"""
Setup script for OGO Car Share Co-op Website
"""
from setuptools import setup

setup(
    name="OGO",
    version="2.0.0",
    install_requires=["setuptools"],
    requires=[],
    packages=["ogo"],
)
